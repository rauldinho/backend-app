{
  "name": "ws-broadcast-demo",
  "version": "1.0.0",
  "description": "WebSocket broadcast demo — deployable to Render",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.8.1"
  }
}
